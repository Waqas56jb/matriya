# Loaded by `npm start` / `npm run dev` (Create React App — development only).
# Overrides `.env` for these keys. Production uses `.env.production` + Vercel env.
REACT_APP_API_BASE_URL=http://localhost:8000
REACT_APP_MANAGEMENT_API_URL=http://localhost:8001
REACT_APP_MANAGEMENT_FRONT_URL=http://localhost:5173

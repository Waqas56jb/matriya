================================================================================
MATRIYA — OWNERSHIP & SOURCE OF TRUTH (David)
Prepared: 2026-04-27 | Written baseline for repo / deploy / infra. No deploy run.
================================================================================

NAMES (roles)
-------------
• Product / client owner: David  
• Primary engineering delivery: Waqas (developer; verify “2nd owner” formally with David)  

================================================================================
IMPORTANT: FOLDER VS REPO (local confusion clarified)
================================================================================
• The **production backend source** is NOT a standalone clone named only “matriya-back repo.”  
• **`matriya-back/` on disk is a DIRECTORY inside monorepo**  
  **`Matriya-System-Project` → https://github.com/shoshan19-prog/Matriya-System-Project**  
• Any local folder labeled “matriya-back” that is its **own** git root is a **different checkout** unless `git remote` matches the table below.  
• **Railway builds from `./matriya-back/` (and root config) inside this monorepo** when connected to GitHub.


================================================================================
SOURCE-OF-TRUTH TABLE — REPO · BRANCH · COMMIT · URLS · DB
================================================================================
Values below are from repository files + git at author time unless marked “VERIFY IN CLOUD”.
Do not treat Railway/Vercel “last deployed SHA” as authoritative without Dashboard check.

SERVICE          | REPO (GitHub)                          | BRANCH | LATEST COMMIT (local GitHub sync)          | DEPLOYMENT URL | DB / STORAGE | STATUS | OWNER / 2ND
-----------------|----------------------------------------|--------|--------------------------------------------|----------------|--------------|--------|--------------
matriya-back (core API, RAG, Matriya GPT sync, WhatsApp webhook, SPA static hosting if public/ bundled) | shoshan19-prog / **Matriya-System-Project** | **main** | **dd02f19** “fix: interpretive questions require explicit doc or [הנחה] tag” (`dd02f19b95d4d975a0fe7ab5212680d9e0418e64`) | **https://matriya-system-project-production.up.railway.app** | **Supabase PostgreSQL** (`rag_documents`, app tables) — pooler typical | Active (per committed URLs) | David / Waqas
matriya-front (React CRA SPA, often built into matriya-back `public/` for same-origin) | Same monorepo | **main** | **dd02f19** | **SPA served from Railway backend** (`/`) + optional **https://matriya-system-project.vercel.app** (`REACT_APP_*` baked at build — see frontend `.env.production`) | n/a (calls APIs) | Active | David / Waqas
managment-back (Management API: projects, lab bridge, GPT RAG for projects, SharePoint/email, etc.) | Same monorepo | **main** | **dd02f19** (same repo HEAD) — **deploy commit MUST be confirmed in Railway** | **https://steadfast-success-production-02d1.up.railway.app** | **Supabase** (often same **project ref** + Storage buckets) — **lab `/api/lab/query`** uses Postgres URL if set | Active (per committed URLs) | David / Waqas
managment-front (Management UI, Vite) | Same monorepo | **main** | **dd02f19** (same repo HEAD) | **Typically Vercel-style host** — `VITE_*` URLs in `managment-front/.env.production` — **exact production hostname: VERIFY Dashboard** | n/a | Active | David / Waqas
(Optional) matriya-finance / other Railway services — **FINANCE_API_URL…** referenced in env docs | Same or separate repo **not fixed in ownership.txt** — **VERIFY** | — | — | **https://captivating-liberation-production-3086.up.railway.app** (placeholder in codebase comments) | — | VERIFY | —


================================================================================
DAVID’S CHECKLIST — ANSWERED POINT-BY-POINT
================================================================================
1. **Which repo is the real production backend?**  
   → **`https://github.com/shoshan19-prog/Matriya-System-Project`** — backend code paths: **`matriya-back/`** (Matriya API) & **`managment-back/`** (Management API). Railway hosts each separately.

2. **Which branch does production deploy from?**  
   → **`main`** (configured in repo; Railway must be set to **`main`** in project settings — VERIFY in Railway UI).

3. **Latest deployed commit**  
   → **Locally & origin `main`: `dd02f19`** / full SHA `dd02f19b95d4d975a0fe7ab5212680d9e0418e64`.  
   → **Deployed SHA on Railway:** open **Railway → project → Deployments → active deployment → commit**. Must match expectation after each push.

4. **Whether all latest code is pushed to GitHub**  
   → **Yes at last check:** branch **up to date with `origin/main`**, working tree **clean**.

5. **Whether any local-only code exists on Waqas’s machine**  
   → **Cannot be guaranteed remotely.** On this checkout: **nothing uncommitted.** Any other folder/machine may drift — use **fresh clone + same remote** to reconcile.

6. **Whether staging/preview exists**  
   → **Not documented as a separate stable environment** in committed production env files — **assume production URLs only**. Vercel may create preview deployments per branch/PR → **VERIFY in Vercel** if previews are used policy-wise.

7. **Which Supabase project is production**  
   → **Production project ref embedded in committed client config:** **`osrcrdroyhlvrtwpybtr`** (URL shape `https://osrcrdroyhlvrtwpybtr.supabase.co`). Aligns DB + anon client usage for management front uploads.

8. **Which Supabase project is staging/dev**  
   → **None named in repo** as a second distinct project URL for staging. Developers may use local `.env` against same pooler/direct URL — treat as **“dev configs point at same Supabase unless you create a separate project.”** VERIFY team policy.

9. **Env var NAMES only (no values)** — grouped by area (subset; full matrices in **`CLAUDE.md`** + per-service starters)

   **matriya-back (representative)**  
   `JWT_SECRET`, `API_PORT`, `API_HOST`, `POSTGRES_URL`, `POSTGRES_PRISMA_URL`, `SUPABASE_DB_URL`, `SUPABASE_URL`, `SUPABASE_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `OPENAI_API_KEY`, `OPENAI_API_BASE`, `OPENAI_RAG_MODEL`, `MATRIYA_OPENAI_VECTOR_STORE_ID`, `LLM_PROVIDER`, `TOGETHER_API_KEY`, `TOGETHER_MODEL`, `HF_API_TOKEN`, `HF_MODEL`, `EMBEDDING_MODEL`, `MATRIYA_LLM_TEMPERATURE`, `EXPRESS_BODY_LIMIT`, `COLLECTION_NAME`, `UPLOAD_DIR`, `MAX_FILE_SIZE`, `CHUNK_SIZE`, `CHUNK_OVERLAP`, `MANAGEMENT_BACK_URL`, `MATRIYA_MANAGEMENT_API_URL`, `MATRIYA_MANAGEMENT_MATERIALS_KEY`, `TWILIO_*`, `*_WHATSAPP*`, `MATRIYA_PROVISION_SECRET`, `MATRIYA_INTERNAL_KEY`, `FINANCE_API_URL`, `MATRIYA_DISABLE_PUBLIC_SIGNUP` / `DISABLE_MATRIYA_PUBLIC_SIGNUP`, `MATRIYA_*` retrieval gates, integrity flags (`B_INTEGRITY_*`, etc.) — extensive list → **`CLAUDE.md`**.

   **matriya-front**  
   `REACT_APP_API_BASE_URL`, `REACT_APP_MANAGEMENT_API_URL`, `REACT_APP_MANAGEMENT_FRONT_URL`

   **managment-back**  
   `PORT`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `MATRIYA_BACK_URL`, `OPENAI_API_KEY`, `OPENAI_RAG_MODEL`, `POSTGRES_URL`, `DATABASE_URL`, `CORS_ORIGINS`, `CORS_ALLOW_VERCEL_PREVIEWS`, `RESEND_*`, `SHAREPOINT_*`, `MANEGER_*` / `MANAGEMENT_*` legacy names, rate limits, `PUBLIC_API_BASE_URL`, etc. — **`CLAUDE.md`**.

   **managment-front**  
   `VITE_MANEGER_API_URL`, `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`

10. **Whether `vercel-env-matriya-back.txt` ever contained real secrets**  
   → **As committed in repo today:** file is a **template** — placeholders like `your-openai-key`, `your-jwt-secret`, `your-supabase-anon-key`, generic `PROJECT_REF` URLs. **It is not the live secret store.**  
   → **If it was ever edited locally** with real keys and committed, that would be a **security incident** — **check git history** for that file. **Do not paste values into chat.**


================================================================================
SECURITY — WHY THIS FILE DOES NOT CONTAIN API KEYS OR PASSWORDS
================================================================================
• **Real secrets (OpenAI, Twilio, JWT, DB URLs, service role keys, etc.) must NEVER be committed to GitHub** — anyone with repo access could steal them; history is forever.  
• **“100% ownership” for David = legal + billing + account access**, not a text file of secrets in the repo.  
• **Correct handover:** David (or his org) holds **owner** on: GitHub org/repo, **Railway** projects, **Vercel** projects, **Supabase** project, **OpenAI** API billing, **Twilio** account, **Resend** (if used), **Azure** (SharePoint). Waqas uses **“Transfer” / add David as owner** in each dashboard, then **rotate** any key that ever lived on a contractor machine.  
• **Where values actually live today:** **Railway** → Service → Variables (matriya-back, managment-back). **Vercel** → Project → Environment Variables (frontends). **Supabase** → Project Settings → API / Database. **OpenAI platform** → API keys. **Twilio** → Console.  
• This document lists **names and purposes only** — the **inventory of “everything that exists”** without creating a leak.


================================================================================
LOCAL .ENV FILES (NOT IN GIT — TYPICAL PATHS)
================================================================================
These paths are **gitignored** (see root `.gitignore`). Developers keep **one file per service** on disk; production uses **cloud env**, not these files on the server.

| Service | Typical local file | Notes |
|---------|-------------------|--------|
| matriya-back | `matriya-back/.env` | Full backend: DB, JWT, OpenAI, Twilio, management URLs, etc. |
| managment-back | `managment-back/.env` | Supabase, OpenAI, Postgres for lab bridge, Resend, SharePoint, CORS. |
| matriya-front | `matriya-front/.env` / `.env.production` | `REACT_APP_*` — some committed template URLs only; **no secrets** in public client env except **anon** key is public by design if present. |
| managment-front | `managment-front/.env` / `.env.production` | `VITE_*` — same note on Supabase anon. |

**Committed templates (placeholders only, not live secrets):**  
`matriya-back/vercel-env-matriya-back.txt`, `managment-back/vercel-env-maneger-back.txt` — copy-paste **names** for Vercel/Railway UI; replace values in the dashboard, not in git.


================================================================================
FULL ENVIRONMENT VARIABLE INVENTORY (NAMES + PURPOSE — NO VALUES)
================================================================================
Source: `CLAUDE.md` + codebase. “Req” = required for typical production.

--- matriya-front ---
REACT_APP_API_BASE_URL (Req) — matriya-back base URL  
REACT_APP_MANAGEMENT_API_URL — managment-back base URL  
REACT_APP_MANAGEMENT_FRONT_URL — management UI URL  

--- managment-front ---
VITE_MANEGER_API_URL (Req) — managment-back base URL  
VITE_SUPABASE_URL — Supabase project URL (bucket upload from browser)  
VITE_SUPABASE_ANON_KEY — Supabase **anon** key (public in client; still rotate if repo is public)  

--- matriya-back (core list) ---
JWT_SECRET (Req) — sign/verify user JWTs  
API_PORT — default 8000  
API_HOST — bind host  
EXPRESS_BODY_LIMIT — JSON body max (e.g. 15mb)  
POSTGRES_URL / POSTGRES_PRISMA_URL / SUPABASE_DB_URL — DB connection (one required)  
SUPABASE_URL — project URL  
SUPABASE_KEY or SUPABASE_SERVICE_ROLE_KEY — server-side Supabase  
OPENAI_API_KEY — RAG / file_search / Ask Matriya when OpenAI path used  
OPENAI_API_BASE — API base URL override  
OPENAI_RAG_MODEL — completion model  
MATRIYA_OPENAI_VECTOR_STORE_ID — OpenAI vector store id  
LLM_PROVIDER — together | huggingface | openai (per config)  
TOGETHER_API_KEY, TOGETHER_MODEL — Together AI  
HF_API_TOKEN, HF_MODEL — Hugging Face  
EMBEDDING_MODEL — local embedding model name  
MATRIYA_LLM_TEMPERATURE — default 0  
UPLOAD_DIR, MAX_FILE_SIZE, CHUNK_SIZE, CHUNK_OVERLAP  
COLLECTION_NAME — pgvector logical collection (`rag_documents`)  
CHROMA_DB_PATH — legacy/local chroma path if referenced  
MANAGEMENT_BACK_URL / MATRIYA_MANAGEMENT_API_URL — managment-back URL for lab/materials bridges  
MATRIYA_MANAGEMENT_MATERIALS_KEY — shared secret header for materials API  
MATRIYA_PRE_LLM_MIN_SIMILARITY, MATRIYA_PRE_LLM_MIN_CHUNKS  
MATRIYA_RETRIEVAL_SIMILARITY_THRESHOLD  
MATRIYA_MAX_ATTRIBUTION_SOURCES  
MATRIYA_DOMAIN_MIN_QUERY_OVERLAP  
MATRIYA_GENERATION_MIN_CHUNKS, MATRIYA_GENERATION_MIN_TOPK_SIMILARITY_SUM, MATRIYA_GENERATION_TOPK_SUM_K  
B_INTEGRITY_MAX_GROWTH_RATIO, B_INTEGRITY_NO_PROGRESS_CYCLES, B_INTEGRITY_METRIC_CAP, B_INTEGRITY_MAX_DROP_PERCENT  
KERNEL_V16_STRICT  
USE_OPENAI_FILE_SEARCH — enables OpenAI file_search + auto-sync path (see `openaiMatriyaConfig.js`)  
VERCEL, VERCEL_ENV, NODE_ENV — runtime detection  
TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN, TWILIO_WHATSAPP_FROM, TWILIO_WHATSAPP_NUMBER, TWILIO_WHATSAPP_REPLY, TWILIO_WEBHOOK_PUBLIC_URL  
DAVID_WHATSAPP, RACHEL_WHATSAPP, WHATSAPP_WHITELIST_ENABLED, SKIP_TWILIO_SIG_CHECK  
MATRIYA_PROVISION_SECRET, MATRIYA_INTERNAL_KEY — cross-service provisioning / internal hooks  
FINANCE_API_URL — optional finance microservice  
MATRIYA_DISABLE_PUBLIC_SIGNUP / DISABLE_MATRIYA_PUBLIC_SIGNUP — signup policy  
(Any additional `MATRIYA_*` tuning vars — scan `matriya-back/config.js` and `server.js` if a key is missing here.)  

--- managment-back ---
PORT — listen port (8001 local)  
SUPABASE_URL / NEXT_PUBLIC_SUPABASE_URL (Req)  
SUPABASE_SERVICE_ROLE_KEY (Req) — **secret** — server only  
MATRIYA_BACK_URL — matriya-back URL for auth proxy  
MANEGER_MATERIALS_SUMMARY_SERVER_KEY — materials summary key  
OPENAI_API_KEY, OPENAI_RAG_MODEL — project GPT RAG  
MANEGER_GPT_RAG_*, GPT_RAG_AUTO_SYNC_DEBOUNCE_MS — sync tuning  
RESEND_API_KEY, RESEND_FROM_EMAIL, RESEND_INBOUND_WEBHOOK_SECRET, RESEND_REPLY_DOMAIN  
PUBLIC_API_BASE_URL — public API URL for webhooks/email  
SHAREPOINT_TENANT_ID, SHAREPOINT_CLIENT_ID, SHAREPOINT_CLIENT_SECRET  
CORS_ORIGINS, CORS_ALLOW_VERCEL_PREVIEWS  
AUTH_LOGIN_RATE_LIMIT_MAX, DISABLE_AUTH_RATE_LIMIT  
UPLOAD_RATE_LIMIT_MAX, API_RATE_LIMIT_MAX  
POSTGRES_URL / DATABASE_URL — lab bridge SQL  

--- Cross-cutting ---
All **values** for the above: **export from Railway/Vercel/Supabase/OpenAI/Twilio dashboards** after David has owner access — **do not** rely on a single `.txt` in the repo for secrets.


================================================================================
FOR DAVID — “YOU HAVE THE ASSET” CHECKLIST (ACCESS, NOT PLAINTEXT KEYS IN GIT)
================================================================================
□ GitHub: **Owner** or **Admin** on `shoshan19-prog/Matriya-System-Project`  
□ Railway: **Owner** on services **matriya-system-project-production**, **steadfast-success-production-02d1**, any finance service  
□ Vercel: **Owner** on projects hosting **matriya-system-project** (and management front if used)  
□ Supabase: **Owner** on project **osrcrdroyhlvrtwpybtr** (or successor) — Database + API keys + Storage  
□ OpenAI: **Billing admin** for API keys used by Matriya + Management RAG  
□ Twilio: **Account owner** for WhatsApp / numbers / webhooks  
□ Optional: Resend, Azure AD (SharePoint), domain DNS for Reply-To  
After access: **rotate** keys that were on contractor laptops; **document** new storage in a **private** password manager (1Password, Bitwarden) — **not** in `ownership.txt`.


================================================================================
END
================================================================================

# Applied when NODE_ENV is not "production" (local `npm start` / `node server.js`).
# Overrides `.env` so auth proxies to your local Matriya API instead of Vercel.
MATRIYA_BACK_URL=http://localhost:8000
